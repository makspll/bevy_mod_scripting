# now

>  Returns an instant corresponding to "now".
>  # Examples
>  ```
>  use std::time::Instant;
>  let now = Instant::now();
>  ```

#### Arguments



#### Returns

- **arg0** : `Instant` \- No Documentation 🚧